## 유효성 검사
- 전달 받은 값이 다음 로직에서 사용되기에 적합한 값인지 아닌지 구별하기 위한 기능

## 유효성 검사의 종류
- 클라이언트에서 전달 받은 값의 유효성을 검사하는 것.
- 데이터베이스에 넣을 값의 형식을 검사하는 것.

### 클라이언트에서 전달 받았을 때
- 기본적으로 유효성 검사는 클라이언트에서 전달 받은 값을 서버의 로직에서 사용해도 되는지를 검사하는 기능이다.

### 데이터베이스에 값을 전달할 때


## 라라벨에서의 유효성 
